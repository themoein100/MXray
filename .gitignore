# Xcode / SwiftPM build artifacts
.build/
.build-ios/
.swiftpm/
DerivedData/
*.xcuserstate
xcuserdata/

# The LibXray binary is distributed via GitHub Releases, never committed
# (each slice exceeds GitHub's 100 MB per-file limit).
Frameworks/LibXray.xcframework
Frameworks/*.zip

# Transient files produced by the auto-release workflow
/LibXray.xcframework.zip
/release-notes.md
/libxray-src/

# macOS
.DS_Store
